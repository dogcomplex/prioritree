<?php

return [
	// '__value'		=> null,
		// the config itself (or pointer to [part of] another config, as appropriate)
	// '__config' 		=> null,
		// with default values here, equals ../__config, but could easily be a subset of the whole config (e.g. no __extend in the config)

	//'__function'		=> function() {
		// builds an config from parent resource, using ../__config, ../__file, ../__db (?), ../__get etc to build it (might be delegateable to just __get?).  Builds according to the parameters in here.
	//},
	'__service'			=> '\Config',
	'merge_layers' 		=> false,
		// whether to go through whole __extend tree and merge
	'depth' 			=> null,
		// how deep to recurse the whole config tree of '__extend's, expressed as a number (non-numeric continues until complete)
		// TODO uhhh.. different parameters at each depth..?  
	'whitelist' 		=> true,
		// config. which fields to explicitly include in config (null or true is all)
	'blacklist'			=> false,
		// config. which fields to exclude in config (null if false is none)
		// TODO validate these two comparatively

	// FUNCTIONS:
	//'to_config'			=> include __DIR__ . '/Array/ToArray.cfg'
	//	'remove_keywords' 	=> false, 	
		// whether to remove all __keywords from final config
 
 
  // test
		
];
